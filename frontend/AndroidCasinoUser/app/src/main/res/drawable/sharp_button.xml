

<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/frostNovaGrey"/>
    <corners android:radius="0dp"/>

    <!-- Optionally you can add a border -->
    <stroke
        android:color="@color/frostNovaOrange"
        android:width="1dp"/>
</shape>